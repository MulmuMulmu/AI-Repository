# PaddleOCR 영수증 식품명 인식 시스템
# Python 3.8 ~ 3.11 권장

# PaddlePaddle (CPU 버전, GPU 사용 시 paddlepaddle-gpu로 교체)
paddlepaddle
paddleocr

# 이미지 처리
Pillow>=10.0.0
opencv-python>=4.8.0

# 데이터 처리 및 분석
numpy>=1.22
pandas>=2.0.0

# 시각화
matplotlib>=3.7.0

# Qwen 보조(OpenAI 호환 API: DashScope, Ollama 등)
openai>=1.12.0
python-dotenv>=1.0.0
